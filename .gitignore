# build / deps
node_modules/
dist/
.astro/
.wrangler/

# env
.env
.env.*
!.env.example
.dev.vars

# OS / editors
.DS_Store
.vscode/
.idea/

# npm is the lockfile of record (CI uses npm ci); local pnpm leftovers are ignored
pnpm-lock.yaml
pnpm-workspace.yaml
